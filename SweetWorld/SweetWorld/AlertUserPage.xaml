<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SweetWorld.AlertUserPage" Title="Уведомления">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <ListView x:Name="alertsList" HasUnevenRows="True" SeparatorColor="Black"  ItemsSource="{Binding}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout  Orientation="Horizontal"  HeightRequest="100"  Padding="0" HorizontalOptions="CenterAndExpand">
                                    <Label Text="Ваш заказ" />
                                    <Label Text="{Binding NameAssortment}"/>
                                    <Label Text="на сумму"/>
                                    <Label Text="{Binding Summa}"/>
                                    <Label Text="{Binding Event}"/>
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>